# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in claims.gemspec
gemspec

gem 'rubocop'
gem 'rubocop-rspec'
gem 'simplecov', require: false, group: :test
